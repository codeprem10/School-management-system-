# Company Notes

## Student Module

- Uses DataGrid
- Uses feature-based modules
- Uses StudentHeaderBar
- Uses StudentActionsCell
- Uses constants file
- Uses shared models
- Uses Redux Observable (Epics)

---

## Important Decisions

- Feature-based folder structure
- Shared Models outside App
- Shared Axios instance
- Material UI latest version
- DummyJSON used as temporary backend
- Typed Redux Hooks instead of raw useDispatch/useSelector
- Learn Redux Observable (Epics) after project completion